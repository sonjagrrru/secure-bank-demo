[Definition]
# Fail2ban filter za Nginx banking app
# Detektuje brute force napade na login endpoint

failregex = ^<HOST> .* "POST /api/auth/login HTTP/1\.(0|1)" (400|401) .*$
            ^<HOST> .* "POST /api/auth/login HTTP/1\.(0|1)" 429 .*$

ignoreregex =
