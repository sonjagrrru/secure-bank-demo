[Definition]
# Fail2ban filter for Nginx banking app
# Detects brute force attacks on login endpoint

failregex = ^<HOST> .* "POST /api/auth/login HTTP/1\.(0|1)" (400|401) .*$
            ^<HOST> .* "POST /api/auth/login HTTP/1\.(0|1)" 429 .*$

ignoreregex =
